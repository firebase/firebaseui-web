/* eslint-disable */
/* tslint:disable */
// @ts-nocheck
import { Components, JSX } from 'firebaseui';


interface MicrosoftAuthButtonProps {
  
}

interface MicrosoftAuthButtonEvents {
  
}

interface MicrosoftAuthButtonSlots {
  default: any
}
  
/* generated by Svelte v3.34.0 */
import {
	SvelteComponent,
	binding_callbacks,
	create_slot,
	detach,
	element,
	init,
	insert,
	safe_not_equal,
	transition_in,
	transition_out,
	update_slot
} from "svelte/internal";

import { createEventDispatcher, onMount } from "svelte";

function create_fragment(ctx) {
	let microsoft_auth_button;
	let current;
	const default_slot_template = /*#slots*/ ctx[3].default;
	const default_slot = create_slot(default_slot_template, ctx, /*$$scope*/ ctx[2], null);

	return {
		c() {
			microsoft_auth_button = element("microsoft-auth-button");
			if (default_slot) default_slot.c();
		},
		m(target, anchor) {
			insert(target, microsoft_auth_button, anchor);

			if (default_slot) {
				default_slot.m(microsoft_auth_button, null);
			}

			/*microsoft_auth_button_binding*/ ctx[4](microsoft_auth_button);
			current = true;
		},
		p(ctx, [dirty]) {
			if (default_slot) {
				if (default_slot.p && dirty & /*$$scope*/ 4) {
					update_slot(default_slot, default_slot_template, ctx, /*$$scope*/ ctx[2], dirty, null, null);
				}
			}
		},
		i(local) {
			if (current) return;
			transition_in(default_slot, local);
			current = true;
		},
		o(local) {
			transition_out(default_slot, local);
			current = false;
		},
		d(detaching) {
			if (detaching) detach(microsoft_auth_button);
			if (default_slot) default_slot.d(detaching);
			/*microsoft_auth_button_binding*/ ctx[4](null);
		}
	};
}

function instance($$self, $$props, $$invalidate) {
	let { $$slots: slots = {}, $$scope } = $$props;
	let __ref;
	let __mounted = false;
	const dispatch = createEventDispatcher();
	const getWebComponent = () => __ref;

	onMount(() => {
		__mounted = true;
	});

	const setProp = (prop, value) => {
		if (__ref) $$invalidate(0, __ref[prop] = value, __ref);
	};

	const onEvent = e => {
		e.stopPropagation();
		dispatch(e.type, e.detail);
	};

	function microsoft_auth_button_binding($$value) {
		binding_callbacks[$$value ? "unshift" : "push"](() => {
			__ref = $$value;
			$$invalidate(0, __ref);
		});
	}

	$$self.$$set = $$props => {
		if ("$$scope" in $$props) $$invalidate(2, $$scope = $$props.$$scope);
	};

	return [__ref, getWebComponent, $$scope, slots, microsoft_auth_button_binding];
}

class MicrosoftAuthButton extends SvelteComponent {
  $$prop_def: MicrosoftAuthButtonProps;
  $$events_def: MicrosoftAuthButtonEvents;
  $$slot_def: MicrosoftAuthButtonSlots;

  $on<K extends keyof MicrosoftAuthButtonEvents>(type: K, callback: (e: MicrosoftAuthButtonEvents[K]) => any): () => void {
	  return super.$on(type, callback);
	}

  $set($$props: Partial<MicrosoftAuthButtonProps>): void {
	  super.$set($$props);
	}

	constructor(options) {
		super();
		init(this, options, instance, create_fragment, safe_not_equal, { getWebComponent: 1 });
	}

	get getWebComponent(): HTMLMicrosoftAuthButtonElement | undefined {
		return this.$$.ctx[1];
	}
}

export default MicrosoftAuthButton;