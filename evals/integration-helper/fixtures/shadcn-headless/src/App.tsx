import { Button } from "./components/ui/button";

export function App() {
  return (
    <main className="shell">
      <h1>Sign in</h1>
      <p>Use the existing Button component when building the sign-in panel.</p>
      <Button>Continue</Button>
    </main>
  );
}

